# Copy to cameras.yaml (gitignored) and fill in real URLs + credentials.
# animation_at is the local time (HH:MM) the day's timelapse is encoded.

cameras:
  - name: whitianga
    url: "http://username:password@127.0.0.1/GetSnapshot"
    animation_at: "22:10"

  - name: whangamata
    url: "http://username:password@127.0.0.1/GetSnapshot"
    animation_at: "22:16"

  - name: thames
    url: "http://username:password@127.0.0.1/GetSnapshot"
    animation_at: "22:21"
